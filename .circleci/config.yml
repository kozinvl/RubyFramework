version: 2.1
jobs:
  build:
    machine: true
    working_directory: ~/repo

    steps:
      - checkout
      - run: docker-compose build
      - run: docker-compose run bbt

workflows:
  version: 2.1
  ui_testing:
    jobs:
      - build


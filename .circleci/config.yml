version: 2
jobs:
  build:
    machine: true
    working_directory: ~/repo

    steps:
      - checkout
      - run: docker-compose build
      - run: docker-compose up
      - run: docker-compose stop

workflows:
  version: 2
  ui_testing:
    jobs:
      - build

